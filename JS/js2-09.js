function getExtremeElements(array) {
  // Change code below this line
  let FirstElements = array[0]
  let LastElement = array[array.length -1]
  
  const NewMassiv = [FirstElements, LastElement]
  
  return NewMassiv
  // Change code above this line
}
